[build]
  base = "Crazy Build"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"
  NITRO_PRESET = "netlify"

[dev]
  command = "npm run dev:app -- --host 0.0.0.0"
  targetPort = 8080
  framework = "#custom"
